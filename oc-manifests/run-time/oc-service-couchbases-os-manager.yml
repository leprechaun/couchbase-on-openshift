apiVersion: v1
kind: Service
metadata:
  name: couchbase-os-manager
  namespace: test-project
spec:
  ports:
    - name: web
      protocol: TCP
      port: 80
      targetPort: 8091
  selector:
    deploymentconfig: couchbase-os-manager
  type: ClusterIP
  sessionAffinity: None
